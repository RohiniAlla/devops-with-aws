name: Upload to S3

on:
  push:
    branches:
      - main

jobs:
  Deploy:
    runs-on: ubuntu-20.04

    steps:
    
    - name: aws deploy s3
      id: devops-with-aws
      uses: RohiniAlla/devops-with-aws@main
      with:
        AWS_SECRET_ID: AKIA2SB2U2MGUC6DKP2C
        AWS_SECRET_KEY: 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
        aws-region: us-east-2
        bucket: aws-devops-cicddemo2
        source: .
        target: s3://aws-devops-cicddemo2 
